{
  "name": "unimar_tms",
  "version": "0.1.0",
  "private": true,
  "description": "Sistema de Gestión de Transportes — Unimar",
  "scripts": {
    "validate": "node .harness/scripts/validate-docs.mjs",
    "validate:satellite": "node .harness/scripts/validate-satellite-base.mjs",
    "lint:md": "markdownlint '**/*.md' --ignore _bmad --ignore .claude --ignore node_modules",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "husky": "^9.0.0",
    "markdownlint-cli": "^0.39.0"
  }
}
